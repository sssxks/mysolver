[package]
name = "qfuf"
version = "0.1.0"
edition = "2024"

[dependencies]
euf = { path = "../euf" }
sat = { path = "../sat" }

[lints.clippy]
dbg_macro = "warn"
todo = "warn"
missing_docs_in_private_items = "warn"

[lints.rust]
missing_docs = "warn"
